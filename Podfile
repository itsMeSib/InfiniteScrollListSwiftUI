# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InfiniteScrolling' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for InfiniteScrolling

  pod 'Kingfisher', '~> 7.0'

  target 'InfiniteScrollingTests' do
    inherit! :search_paths
    pod 'Quick'
    pod 'Nimble'
  end

  target 'InfiniteScrollingUITests' do
    # Pods for testing
  end

end
